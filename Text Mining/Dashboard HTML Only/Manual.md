# LLM Hermeneutic Workbench 使用手册

本手册帮助你快速了解与使用语义三元组可视化分析 Dashboard。

---

## 1. 功能概述

该 Dashboard 将访谈文本中的"语义三元组"（主语→关系→宾语）以可视化方式展示，支持：

- **实体类型分析**：按出现频率分三级显示（>3次、2-3次、1次）
- **结构模式统计**：最常见的 Subject→Relation→Object 模式
- **交互式过滤**：按类型、关系、模式或关键词筛选
- **网络可视化**：以图形方式探索实体间的关系网络
- **数据导出**：CSV/JSON 格式导出

---

## 2. 界面布局

### 顶部导航栏
| 元素 | 功能 |
|------|------|
| **View Layers** | 切换分析视图层级（Discourse/Narrative/Claim/Historical） |
| **Statistics** | 统计信息（待实现） |
| **Models & API** | 模型配置（待实现） |
| **Edit Ontology** | 本体编辑（待实现） |
| **🔍 搜索图标** | 点击展开搜索栏，输入实体名/类型/关系关键词 |

### 全局统计卡片
显示 5 项汇总指标：提取总数、话轮数、唯一实体数、实体类型数、关系数

### 主内容区（左侧）
- 按话轮（Speaker Turn）展示三元组
- 每个卡片显示：主语实体、关系、宾语实体、证据文本

### 侧边栏（右侧）
1. **Export 面板**：勾选导出项后点击"Export Selected"
2. **Entity Types 面板**：按频率分组的可点击实体类型标签
3. **Pattern Analytics 面板**（可折叠）：
   - Entity Type Patterns：多类型实体、仅主语/宾语类型
   - Relation Cardinality：1:1、1:N、N:1 关系模式
   - Structural Patterns：高频结构模式列表

### 固定按钮
- **Visualise**：打开网络可视化模态框
- **Back to Top**：返回页面顶部

---

## 3. 交互操作

| 操作 | 方法 |
|------|------|
| 搜索 | 点击导航栏搜索图标，输入关键词后按 Enter |
| 按类型筛选 | 点击右侧 Entity Types 中的标签（可多选） |
| 按关系筛选 | 点击 Pattern Analytics 中的关系代码 |
| 按模式筛选 | 点击 Structural Patterns 列表中的某一行 |
| 清除筛选 | 点击筛选状态栏中的 "Clear All" 按钮 |
| 导出数据 | 在 Export 面板勾选后点击"Export Selected" |

> **提示**：筛选条件会实时反映在导航栏下方的筛选状态区

---

## 4. 网络可视化

点击 **Visualise** 按钮进入全屏网络视图：

### 控制栏
| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| Search | 搜索节点名称 | - |
| Physics | 开关物理引擎（节点自动布局） | P |
| Labels | 切换节点/边标签显示 | L |
| Hops | 扩展跳数（0=仅匹配节点，+1/+2=向外扩展邻居） | - |
| Fit | 自动适应视野 | F |
| Cluster | 按实体类型聚类 | C |
| Export | 导出为 PNG/JSON/CSV | - |

### 交互方式
- 🖱️ 滚轮缩放，拖拽平移
- 点击节点高亮邻居关系
- 点击空白处取消选中

---

## 5. 离线使用

| 功能 | 离线可用性 |
|------|-----------|
| 页面浏览与筛选 | ✅ 完全可用 |
| 网络可视化 | ⚠️ 需联网加载 vis.js 库 |
| 字体 | ⚠️ 离线时回退系统字体，不影响功能 |

**完全离线方案**：联系技术人员将 `lib/vis-9.1.2/vis-network.min.js` 改为本地引用。

---

## 6. 常见问题

| 问题 | 解决方案 |
|------|---------|
| Visualise 按钮无反应/空白 | 检查网络连接，或改用本地 vis.js |
| 筛选结果为空 | 点击 "Clear All" 清除所有筛选条件 |
| 导出文件在哪里 | 查看浏览器下载目录 |
| 页面卡顿 | 减少筛选范围，关闭 Physics，降低 Hops |
| 字体显示不一致 | 离线/内网时正常现象，不影响功能 |

---

## 7. 术语速查

| 术语 | 说明 |
|------|------|
| 语义三元组 | Subject（主语）→ Relation（关系）→ Object（宾语） |
| 话轮 | 一次发言，包含若干三元组 |
| 结构模式 | SubjectType → Relation → ObjectType 的统计 |
| Hops | 从选中节点向外扩展的跳数 |

---

## 附录：技术人员参考

### 环境要求
```bash
# Python 3.12+
pip install jinja2
```

### 生成报告
```bash
python3 generate_report_modern.py -i input.json -o output.html
```

### 项目结构
```
Dashboard 1205/
├── generate_report_modern.py    # 报告生成脚本
├── template_modern.html.j2      # 主模板
├── templates/partials/          # HTML 组件
│   ├── _navbar.html.j2
│   ├── _global_summary.html.j2
│   ├── _sidebar.html.j2
│   └── _network_modal.html.j2
├── static/css/                  # 样式文件
│   ├── design-tokens.css
│   ├── components.css
│   └── network-modal.css
└── static/js/                   # JavaScript 模块
    ├── utils.js
    ├── filter-engine.js
    ├── network-viz.js
    └── main.js
```

### 输入数据格式
```json
[
  {
    "speaker_name": "Alice",
    "role": "Interviewer",
    "utterance_order": 1,
    "extractions": [
      {
        "subject_entity": {"name": "X", "entity_type": "Person"},
        "relation": {"surface_form": "uses", "semantic_form": "uses"},
        "object_entity": {"name": "Y", "entity_type": "Tool"},
        "evidence_sources": ["source_id"],
        "evidence_text": "原文片段"
      }
    ]
  }
]
```

---

*最后更新：2025年12月*
